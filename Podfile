# Even though we're targeting iOS 5, set Cocoapods
# to target iOS 4; otherwise the dependencies are built
# for the incorrect architecture
platform :ios, '5.0'


pod 'ABContactHelper'
pod 'SVProgressHUD'